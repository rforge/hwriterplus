Use Ross' script function to record output instead of txtStart.

Add a meta argument to newPage so that author etc can be added as in
Siegrist (2007) recommendations

Change the headers to match what is in Siegrist (2007)

Allow LaTeXMathML as in Knisley

Fix the equation referencing. Record the equation number and a
label. Use the label to allow for reordering of the equations. Use R code instead of JavaScript, then numbering should also work in Word


